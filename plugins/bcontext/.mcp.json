{
  "mcpServers": {
    "bcontext": {
      "type": "http",
      "url": "${BCONTEXT_URL:-https://app.bcontext.dev}/mcp",
      "headers": {
        "Authorization": "Bearer ${BCONTEXT_TOKEN}",
        "X-Bcontext-Workspace": "${BCONTEXT_WORKSPACE:-}"
      }
    }
  }
}
